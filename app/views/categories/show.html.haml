- cache page_cache_key do
  %h1= @category.name
  != textile(@category.text)
  - unless @category.pages.empty?
    %ul
      - @category.pages.each do |page|
        %li= link_to page.name, page.path
