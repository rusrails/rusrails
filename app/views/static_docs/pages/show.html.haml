- if @page && @page.path == 'index'
  #main_page_banner.well
    = render "layouts/rsya", limit: 2
    = render "shared/vexor"
    =# render "shared/railsclub"

- cache ['v1', @page] do
  = @page.rendered_body(self)

- content_for :menu do
  - cache ['v1', @page, 'menu'] do
    = @page.meta[:menu]
